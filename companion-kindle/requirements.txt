# Runtime deps for the Aquilo Kindle companion. Lean on purpose so the
# PyInstaller single-file exe stays under the size budget. The local
# control server uses the stdlib http.server (no Flask).
requests>=2.32.0
pystray>=0.19.5
Pillow>=10.0.0
packaging>=24.0
selenium>=4.20.0
